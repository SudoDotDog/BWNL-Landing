---
menu: Components
name: Dropdown Navigate Button
---

import { DropdownNavigateButton } from "../../src/index";
import { Playground } from "docz";

# Dropdown Navigate Button

## Simple

<Playground>
    <DropdownNavigateButton 
        title="Without Action"
    >
        <div style={{
            width: '300px', 
            height: '400px', 
            backgroundColor: 'blue',
            color: 'white',
        }}>Menu</div>
    </DropdownNavigateButton>
    <DropdownNavigateButton 
        title="With Action"
        onClick={() => console.log('Yeah!')}
    >
         <div style={{
            width: '300px', 
            height: '400px', 
            backgroundColor: 'blue',
            color: 'white',
        }}>Menu</div>
    </DropdownNavigateButton>
</Playground>
